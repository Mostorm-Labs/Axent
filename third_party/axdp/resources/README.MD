- RUN with CMake for mac:
  ```
   mkdir build
   cd build
   cmake .. -G Xcode
  ```

- RUN with CMake for win:
    ```
    mkdir build
    cd build
    cmake .. -G 'Visual Studio 2019' -A Win32
    ```
